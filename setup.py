from setuptools import setup

setup(
    name='client-server-network',
    version='1.0',
    description='This is a Python package to use the Python socket module to create a client / server network.',
    packages=['clientserver'],
    install_requires=[
    ],
)